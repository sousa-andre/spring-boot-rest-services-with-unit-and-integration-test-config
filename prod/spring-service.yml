apiVersion: v1
Kind: Service
metadata:
    name: spring-service
spec:
    type: LoadBalancer
    selector:
        matchLabels:
            app: spring
    ports:
        - port: 8080
          targetPort: 8080
          nodePort: 8090